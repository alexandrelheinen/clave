# Example telemetry channel configuration for CLAVE hosts.
# Copy to telemetry.yml (gitignored) for local overrides.

channel:
  capacity: 64
  full_mode: wait  # wait | drop_oldest (Phase 1+)

host:
  name: clave-host
  product: CLAVE
