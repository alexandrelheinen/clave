"""The benchmark: every configuration CLAVE can run, scored by one protocol.

Nothing here computes a metric. The validation harness from v0.8.0 owns metrics
and gates, the runtime from v0.9.0 owns the loop, and this package runs one
against the other and reports what comes out.
"""
