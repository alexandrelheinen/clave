# Quick training run for verification and cloud backup.

training:
  candidate: behavior-cloning-baseline
  dataset: datasets/synthetic
  checkpoints: runs/quick
  epochs: 3
  batch_size: 4
  learning_rate: 0.001
  seed: 42
  samples_per_crossing: 3
  accumulation_steps: 1
  class_balance: none
  window_exit_meters: 1.034
  act_chunk_size: 10
