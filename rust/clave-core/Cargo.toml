[package]
name = "clave-core"
version = "0.1.0"
edition = "2021"
publish = false
description = "CLAVE auditable hot-path stubs (SampleRing). FFI deferred to a later phase."
license = "MIT"
repository = "https://github.com/alexandrelheinen/clave"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
